# Admin Module Notes

This project keeps executable MVC files inside:

- `App/controllers/AdminController`
- `App/views/admin`
- `public/css/admin.css`

This `admin/` directory is reserved for future admin-specific assets/scripts if needed.

